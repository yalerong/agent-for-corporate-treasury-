# 企业资金智能体 Phase 1 依赖锁定
# Python >= 3.11
# 锁定基线：2026-05-16

# ── Agent 编排 ──
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langgraph>=0.2,<0.5

# ── LLM 客户端（OpenAI 协议兼容层，覆盖 OpenAI / DeepSeek / Qwen 兼容模式）──
langchain-openai>=0.2,<0.4

# ── 向量库与 Embedding ──
qdrant-client>=1.7,<2.0
langchain-qdrant>=0.1,<0.3
langchain-huggingface>=0.1,<0.3
sentence-transformers>=2.5,<4.0

# ── 配置与环境 ──
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# ── 开发工具 ──
pytest>=8.0,<9.0
ruff>=0.6,<1.0

# ── API 服务（Phase 2 接入）──
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0  # FastAPI TestClient 依赖
