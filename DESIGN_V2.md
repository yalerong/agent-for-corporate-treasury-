# 资金智能体 v2 设计 —— 会学习、会自己找数据的资金专员

> 2026-07-31 起草。前置调研：Nilus《Agentic AI for Treasury 2026 Guide》、ChatFin/Kognitos 行业综述、SAGE/Reflexion 等 Agent 记忆-反思论文。
> 定位：替代 agent-for-corporate-treasury-（Phase 0 文档包）的实施蓝图；cash-ops 降级为本系统的"确定性引擎"组件。

---

## 0. 为什么之前的太简单

| 版本 | 缺什么 |
| --- | --- |
| agent-for-corporate-treasury-（旧仓库） | 只有角色扮演架构图（出纳/主管/经理 Agent），没有任何真实数据通道和学习能力。角色隔离是组织概念，不是智能来源。 |
| cash-ops | 有真实数据和审批链路，但是纯确定性：不学习（每周偏差不回流）、不自主（等人喂 xlsx）、不推理（例外全靠人）。 |

**v2 的定义性差异：智能体自己获取信息、自己总结规律、自己提出行动——人只批规则和批例外，不再逐笔搬数。**

业界佐证（2026 生产环境）：agentic 13 周现金预测准确率 88–92%（人工约 60–75%），关键不是模型多聪明，而是三件事：**实时数据直连、预测-实际偏差持续回流、CFO 可配置的分级自主权**。

---

## 1. 三个自主环（智能体的"活"的部分）

### 1.1 感知环 —— 自己获取信息

不再等人导出 xlsx。Agent 按日程自主运行（cron 触发，非人触发）：

- **银行余额**：复用 finweb 五国余额接口的模式（HMAC/MD5 签名、时区两班制、代理约束这些坑都已趟过），每日 EOD 自动落库，带 `data_version` 快照戳（复用 factor-analysis PR#52 的版本戳思路）。
- **资金预算**：Lark Base 只读 API 定时拉取（应用以只读文档应用身份加入 Base——cash-ops 已确认此口径）。
- **付款实际**：从银行流水/ERP 导出解析，作为"实际发生"事实源——这是学习环的燃料。
- **市场数据**：汇率（多币种主体必需）。
- **主动补缺**：发现某部门预算未报 / 某账户余额缺失（NEEDS_BALANCE），不是报错等人，而是①先自查（名称模糊匹配、币种、导出日期），②自查不能解决时起草催办消息发给对应负责人。

### 1.2 学习环 —— 自己总结规律（本设计的核心增量）

**规律库（Pattern Store）**：文件化的结构化记忆，每条规律 = 一个带证据链的 Markdown/YAML 条目：

```yaml
id: dept-ops-budget-bias
claim: 运营部周度预算系统性高报，近12周实际支付/预算均值 0.83（σ=0.06）
evidence: [run-2026-05-04 … run-2026-07-27]   # 可追溯到具体周
sample_n: 12
confidence: high        # n≥8 且方向稳定 → high；否则 provisional
action: 预测时对该部门预算乘修正系数 0.85（保守取上界）
approved_by: 人工 2026-08-xx    # 规律进入计算前必须人批
```

**每周复盘任务（自动）**：周一实际调拨执行后，Agent 对比上周"建议 vs 实际"：

1. 哪些预算高报/低报？→ 更新部门偏差系数
2. 哪些付款延后/提前？→ 更新供应商/项目付款行为画像
3. 哪些账户名匹配失败过？→ 更新账户别名映射
4. 出现过的例外及处置 → 写成案例（下次同类例外先引用先例）
5. 季节规律（月末归集、发薪日、税期）→ 日历型规律

**红线——学习结果如何参与决策**：

- 规律**不直接改数**：以修正系数/别名表/日历标记的形式，作为确定性引擎的**输入参数**。LLM 总结规律，代码执行规律——每个数字仍可追溯。
- `provisional` 规律只出现在报告的"提示"栏，不进计算；升 `high` 且**人工批准**后才进引擎参数。这对应业界的关键约束："系统不在无人授权下改变基础规则"。
- 每条建议记录所用规律版本（pattern store 的 git commit hash），审计时可重放"当时它为什么这么算"。

### 1.3 行动环 —— 分级自主权

| 级别 | 范围 | 例子 |
| --- | --- | --- |
| L0 观察 | 只读+报告 | 每日头寸日报、异动告警（余额偏离计划>阈值） |
| L1 提议 | 生成方案，人逐笔批 | 周度调拨建议、例外处置方案（2-3 个带代价的选项） |
| L2 预备 | 人批后 Agent 执行到审批门 | 确认后自动调 Approval v4 preview→create（preview 失败绝不 create，沿用 cash-ops 红线） |
| L3 有界自主 | 白名单内自动 | 远期：同名账户小额归集等零风险动作。初期不开。 |

付款执行永远不给 Agent 工具——安全靠工具不存在，不靠 prompt。

---

## 2. 架构

```
定时器(cron) ──┐                      ┌── Lark 对话入口（问答/催办/审批推送）
               ▼                      ▼
        ┌─────────────────────────────────┐
        │        Treasury Agent（单 Agent + 任务模式）      │
        │  日常巡检 / 周度规划 / 复盘学习 / 例外处置 / 问答  │
        └───────┬───────────┬─────────────┘
                │ 工具调用    │ 读写
        ┌───────▼─────┐ ┌───▼──────────┐
        │ 确定性引擎    │ │ 规律库 Pattern │
        │ (cash-ops    │ │ Store (git 版本 │
        │  演化, 缺口/  │ │ 化, 人批准入)   │
        │  路由/限额)   │ └──────────────┘
        └───────┬─────┘
        ┌───────▼──────────────────────────┐
        │ 数据层: 余额采集(finweb模式) · 预算 │
        │ Base 拉取 · 付款实际 · 汇率 · 快照戳 │
        └───────┬──────────────────────────┘
        ┌───────▼──────────────────────────┐
        │ 执行层: Approval v4 preview→create │
        │ 审计日志(输入/规律版本/备选/置信度)  │
        └──────────────────────────────────┘
```

**架构判断（与旧设计的两个不同）**：

1. **不做多角色 Agent**。出纳/主管/经理拆成多个 Agent 是模拟组织架构，不是模拟智能——一个 Agent + 按任务切换的工具白名单（巡检模式只读、规划模式可写提案、复盘模式可写规律库）就实现了 SOD，还省掉 Supervisor 路由这层纯开销。等单 Agent 撞到上下文/职责上限再拆不迟。
2. **不做 RAG 三级知识库起步**。法规知识库是旧设计的重心，但对"预算-余额-调拨"闭环贡献为零。规律库（自己生产的知识）价值远高于法规库（网上抄的知识）。法规问答放远期。

**技术栈**：Claude Agent SDK（Python）+ 工具层用 MCP（复用 factor-analysis mcp_server 的能力探测/优雅降级经验）+ SQLite/Postgres 存事实数据 + git 仓库存规律库 + cron 定时唤起。不用 LangGraph——你的多步流程是确定性 pipeline + Agent 例外处理，不需要图编排。

---

## 3. 落地路径（每阶段都有独立交付物）

**Phase 1（2-3 周）：把 cash-ops 变成 Agent 可用的工具集**
- `weekly_transfer_planner.py` 拆为 MCP 工具：`get_balances` / `get_budget` / `compute_gaps` / `run_scenario` / `read_ledger`
- Agent 会话入口：能回答"下周 MXN 够不够"、能跑 what-if
- 交付物：对话式周度规划，替代手动跑脚本

**Phase 2（2-3 周）：感知环**
- 余额自动采集落库（finweb 模式移植）+ 预算 Base 定时拉取
- 每日巡检 cron：头寸日报 + 异动告警推 Lark
- 交付物：数据不再要人导出；Agent 每天自己"看盘"

**Phase 3（3-4 周）：学习环**
- 付款实际回流 + 周一自动复盘任务 + Pattern Store + 人工批准流
- 预测公式升级：`建议调拨 = max(0, 预算×部门修正系数 + 流动性需求 + 最低余额 − 余额) + 日历规律项`
- 交付物：可量化的准确率曲线（建议 vs 实际的周度偏差趋势图）——这是系统"在变聪明"的直接证据，也是最硬的作品集素材

**Phase 4（远期）**：L2 审批直通（preview→create 自动化）、例外案例库、多主体多币种净额优化。

---

## 4. 风险与红线（继承 + 新增）

- 继承 cash-ops 全部红线：不猜账户、preview 失败不 create、form 结构不手工猜、凭据不进仓库（*.example 模板模式）。
- 新增：规律库变更走 git + 人批；`provisional` 规律不进计算；Agent 无付款工具；每条建议钉规律版本 hash；预测-实际偏差连续 2 周恶化 → 自动冻结相关规律回退上一版本。
- LLM 输入脱敏：账号进 LLM 前替换为内部代号（映射表只在本地）。

## 5. 判据：什么叫"不再太简单"

三个月后系统应能通过这三问：
1. 它这周用的数据，有没有任何一步是人手工导出的？（感知环）
2. 它这周的建议，和 12 周前比是不是更准了？拿偏差曲线说话。（学习环）
3. 出现一个没见过的例外时，它是报错停下，还是给出了带先例引用的处置方案？（行动环）
